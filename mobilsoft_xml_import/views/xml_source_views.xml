<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- XML Kaynak Form View -->
    <record id="xml_product_source_form_view" model="ir.ui.view">
        <field name="name">xml.product.source.form</field>
        <field name="model">xml.product.source</field>
        <field name="arch" type="xml">
            <form string="XML Kaynağı">
                <header>
                    <button name="action_test_connection" type="object" string="Bağlantıyı Test Et" class="btn-secondary" invisible="state == 'active'"/>
                    <button name="action_preview_xml" type="object" string="XML Önizle" class="btn-secondary"/>
                    <button name="action_import_products" type="object" string="Ürünleri Şimdi Çek" class="btn-primary" invisible="state not in ('active', 'draft')"/>
                    <button name="action_load_template_mappings" type="object" string="Şablon Yükle" class="btn-secondary" invisible="xml_template == 'custom'"/>
                    <button name="action_apply_dropship_route" type="object" string="🚚 Dropship Rotası Uygula" class="btn-secondary" invisible="product_count == 0"/>
                    <button name="action_sync_suppliers" type="object" string="👤 Tedarikçi Senkronize" class="btn-secondary" invisible="not supplier_id or product_count == 0"/>
                    <button name="action_activate" type="object" string="Aktifleştir" invisible="state == 'active'"/>
                    <button name="action_pause" type="object" string="Duraklat" invisible="state != 'active'"/>
                    <button name="action_reset" type="object" string="Sıfırla" invisible="state == 'draft'"/>
                    <field name="state" widget="statusbar" statusbar_visible="draft,active"/>
                </header>
                <sheet>
                    <div class="oe_button_box" name="button_box">
                        <button name="action_view_products" type="object" class="oe_stat_button" icon="fa-cubes">
                            <field name="product_count" string="Ürünler" widget="statinfo"/>
                        </button>
                        <button name="action_view_logs" type="object" class="oe_stat_button" icon="fa-history">
                            <field name="log_count" string="Loglar" widget="statinfo"/>
                        </button>
                    </div>
                    <widget name="web_ribbon" title="Arşivlendi" bg_color="text-bg-danger" invisible="active"/>
                    <div class="oe_title">
                        <label for="name" string="Kaynak Adı"/>
                        <h1>
                            <field name="name" placeholder="Tedarikçi XML Feed Adı"/>
                        </h1>
                    </div>
                    <group>
                        <group string="XML Bağlantı Ayarları">
                            <field name="xml_url" widget="url" placeholder="https://tedarikci.com/xml/products.xml"/>
                            <field name="xml_template"/>
                            <field name="root_element" placeholder="//Products/Product"/>
                            <field name="xml_username" placeholder="Opsiyonel"/>
                            <field name="xml_password" password="True" placeholder="Opsiyonel"/>
                            <field name="supplier_id"/>
                        </group>
                        <group string="Durum Bilgisi">
                            <field name="active" widget="boolean_toggle"/>
                            <field name="last_sync" readonly="1"/>
                            <field name="next_sync" readonly="1"/>
                            <field name="last_error" readonly="1" invisible="not last_error"/>
                        </group>
                    </group>
                    
                    <notebook>
                        <page string="Alan Eşleştirmeleri" name="mappings">
                            <field name="field_mapping_ids" nolabel="1">
                                <list editable="bottom">
                                    <field name="sequence" widget="handle"/>
                                    <field name="odoo_field"/>
                                    <field name="xml_path" placeholder="ProductCode"/>
                                    <field name="transform"/>
                                    <field name="default_value" placeholder="Varsayılan"/>
                                    <field name="is_required" widget="boolean_toggle"/>
                                </list>
                            </field>
                            <div class="text-muted mt-2">
                                <strong>XML Yolu Örnekleri:</strong>
                                <code>ProductCode</code> (doğrudan element),
                                <code>Category/Name</code> (iç içe),
                                <code>@id</code> (attribute)
                            </div>
                        </page>
                        
                        <page string="Fiyatlandırma" name="pricing">
                            <group>
                                <group string="Kar Marjı Ayarları">
                                    <field name="price_markup_type" widget="radio"/>
                                    <field name="price_markup_percent" invisible="price_markup_type == 'fixed'"/>
                                    <field name="price_markup_fixed" invisible="price_markup_type == 'percent'"/>
                                </group>
                                <group string="Fiyat Yuvarlama">
                                    <field name="price_round" widget="boolean_toggle"/>
                                    <field name="price_round_method" invisible="not price_round"/>
                                </group>
                            </group>
                            <group>
                                <group string="Fiyat/Stok Filtreleri">
                                    <field name="min_price"/>
                                    <field name="max_price"/>
                                    <field name="min_stock"/>
                                </group>
                                <group string="Fiyat Formülü">
                                    <div class="text-muted">
                                        <p><strong>Satış Fiyatı =</strong></p>
                                        <p>(Tedarikçi Fiyatı × (1 + Kar%/100)) + Sabit Kar</p>
                                    </div>
                                </group>
                            </group>
                        </page>
                        
                        <page string="Senkronizasyon" name="sync">
                            <group>
                                <group string="Otomatik Güncelleme">
                                    <field name="auto_sync" widget="boolean_toggle"/>
                                    <field name="sync_interval" invisible="not auto_sync"/>
                                </group>
                                <group string="İçe Aktarım Seçenekleri">
                                    <field name="create_new_products" widget="boolean_toggle"/>
                                    <field name="update_existing" widget="boolean_toggle"/>
                                    <field name="update_price" widget="boolean_toggle" invisible="not update_existing"/>
                                    <field name="update_stock" widget="boolean_toggle" invisible="not update_existing"/>
                                    <field name="update_images" widget="boolean_toggle" invisible="not update_existing"/>
                                </group>
                            </group>
                            <group>
                                <group string="Ürün Eşleştirme Önceliği (Sıralı)">
                                    <field name="match_by_sku_prefix" widget="boolean_toggle" help="1. SKU ilk kelimesi"/>
                                    <field name="match_by_barcode" widget="boolean_toggle" help="2. Barkod"/>
                                    <field name="match_by_sku" widget="boolean_toggle" help="3. SKU tam eşleşme"/>
                                    <field name="match_by_description" widget="boolean_toggle" help="4. Açıklama"/>
                                    <field name="description_match_ratio" invisible="not match_by_description"/>
                                    <field name="match_by_name" widget="boolean_toggle" help="5. İsim"/>
                                    <field name="name_match_ratio" invisible="not match_by_name"/>
                                </group>
                                <group string="Varyant ve Varsayılanlar">
                                    <field name="create_variants" widget="boolean_toggle" help="Aynı SKU prefix farklı barkod = varyant"/>
                                    <field name="default_category_id"/>
                                    <field name="default_product_type"/>
                                </group>
                            </group>
                            <group>
                                <group string="Güncelleme Politikası">
                                    <field name="update_only_if_value" widget="boolean_toggle" help="Boş değer gelirse mevcut değeri koru"/>
                                </group>
                                <group string="Stok Sıfır Politikası">
                                    <field name="deactivate_zero_stock" widget="boolean_toggle"/>
                                    <field name="delete_unsold_zero_stock" widget="boolean_toggle"/>
                                </group>
                            </group>
                        </page>
                        
                        <page string="İçe Aktarım Logları" name="logs">
                            <field name="import_log_ids" readonly="1">
                                <list>
                                    <field name="start_time"/>
                                    <field name="state" widget="badge" decoration-success="state == 'done'" decoration-danger="state == 'error'" decoration-info="state == 'running'"/>
                                    <field name="total_products"/>
                                    <field name="products_created" string="Oluşturulan"/>
                                    <field name="products_updated" string="Güncellenen"/>
                                    <field name="products_skipped" string="Atlanan"/>
                                    <field name="products_failed" string="Hatalı"/>
                                    <field name="duration" string="Süre (sn)"/>
                                </list>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <chatter/>
            </form>
        </field>
    </record>

    <!-- XML Kaynak List View -->
    <record id="xml_product_source_tree_view" model="ir.ui.view">
        <field name="name">xml.product.source.list</field>
        <field name="model">xml.product.source</field>
        <field name="arch" type="xml">
            <list string="XML Kaynakları" decoration-muted="not active">
                <field name="sequence" widget="handle"/>
                <field name="name"/>
                <field name="xml_template" optional="show"/>
                <field name="supplier_id" optional="show"/>
                <field name="product_count" string="Ürün"/>
                <field name="last_sync" optional="show"/>
                <field name="state" widget="badge" decoration-success="state == 'active'" decoration-danger="state == 'error'" decoration-warning="state == 'paused'" decoration-info="state == 'draft'"/>
                <field name="active" column_invisible="True"/>
            </list>
        </field>
    </record>

    <!-- XML Kaynak Search View -->
    <record id="xml_product_source_search_view" model="ir.ui.view">
        <field name="name">xml.product.source.search</field>
        <field name="model">xml.product.source</field>
        <field name="arch" type="xml">
            <search string="XML Kaynakları">
                <field name="name"/>
                <field name="supplier_id"/>
                <filter name="active" string="Aktif" domain="[('state', '=', 'active')]"/>
                <filter name="archived" string="Arşivlenmiş" domain="[('active', '=', False)]"/>
                <separator/>
                <filter name="filter_template" string="Şablona Göre" context="{'group_by': 'xml_template'}"/>
            </search>
        </field>
    </record>

    <!-- XML Kaynak Action -->
    <record id="xml_product_source_action" model="ir.actions.act_window">
        <field name="name">XML Kaynakları</field>
        <field name="res_model">xml.product.source</field>
        <field name="view_mode">list,form</field>
        <field name="search_view_id" ref="xml_product_source_search_view"/>
        <field name="context">{'search_default_active': 1}</field>
        <field name="help" type="html">
            <p class="o_view_nocontent_smiling_face">
                İlk XML kaynağınızı ekleyin!
            </p>
            <p>
                Tedarikçilerinizin XML feed'lerini ekleyerek ürünleri otomatik olarak içe aktarabilirsiniz.
            </p>
        </field>
    </record>

</odoo>
